dist/
node_modules/
.astro/
.DS_Store
npm-debug.log*
.env
.env.production

# Relay CRM prototype (local tool, not part of the marketing site)
RELAY/

# Nested client repo accidentally swept in — keep local, not tracked
Sites/Ember&Oak Media/
