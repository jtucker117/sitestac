dist/
node_modules/
.astro/
.DS_Store
npm-debug.log*
.env
.env.production

# Relay CRM prototype (local tool, not part of the marketing site)
RELAY/

# Nested client repo accidentally swept in — keep local, not tracked
Sites/Ember&Oak Media/

# Screen recordings — 26MB+ each, and git history is a one-way door
*.mov

# Supabase CLI local state — records which project this checkout is linked to
supabase/.temp/
